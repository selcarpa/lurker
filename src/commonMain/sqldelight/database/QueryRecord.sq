CREATE TABLE QueryRecord
(
    queryId    TEXT NOT NULL
        CONSTRAINT query_record_pk
            PRIMARY KEY,
    queryName  TEXT,
    queryType INTEGER,
    plainRequest TEXT,
    createTime INTEGER
    )
;

insert:
INSERT INTO QueryRecord (queryId,queryName,queryType,plainRequest,createTime)VALUES (
UUID(),?,?,?,?
);

